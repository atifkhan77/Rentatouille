import 'package:flutter/material.dart';

const themColor = LinearGradient(
    colors: [Colors.deepPurple, Colors.purple, Colors.pink],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter);
